BUILD INSTRUCTIONS:

NOTE: many mod files are not in source control, they reside in
a tarball that must be extracted before this mod can be built.

- cygstart src/materialsystem/stdshaders/buildmyshaders.bat
- build src/everything.sln
- untar binary files somewhere like $DEVEL/bin/darkness
- copy/modify build.env, source it (set BINDIR to $DEVEL/bin/darkness, for example)
- ./setup_bin.sh
- make darkness
- make darkness.zip
